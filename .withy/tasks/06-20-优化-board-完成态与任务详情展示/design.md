# 设计

- `TaskCard` 将 `done` 列作为终态配色的最高优先级，常态边框和选中 ring 均复用 teal。
- `ViewDetail` 保留详情栏原有的纵向滚动，显式禁止横向溢出。步骤列表随折叠面板在正常文档流中展开，不设置独立高度或滚动容器。
- 使用原生 `details` / `summary` 保留键盘与读屏语义，不增加独立状态。
- 不改动进度解析器；两条内容是说明性文本，将它们从普通列表改为段落，符合 `implement.md` 只用复选框列表表达进度的约定。
