# Board 展示优化

## 目标

- 已完成任务卡片使用绿色边框表达终态。
- 实施计划的进度条始终可见，明细默认收起。
- 展开明细时，右侧详情不产生横向滚动，保留原有的详情栏纵向滚动。
- `implement.md` 的进度提示与文件实际格式一致。

## 验收标准

- 已完成卡片无论是否选中，边框均为 teal。
- 折叠面板收起和展开时，详情外层 `scrollWidth === clientWidth`。
- 长步骤在折叠面板内换行，折叠面板不创建独立滚动区。
- 页面不再显示“2 条列表项未识别为复选框”。
