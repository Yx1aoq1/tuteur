# Developer identity and local runtime state
# (workspace/ is committed — its subdirs are the project member roster)
.developer
runtime/

# `withy update` backups (transient)
backup-update-*/

# Temporary files
*.tmp
*.new
