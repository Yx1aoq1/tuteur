node_modules/
.next/
dist/
coverage/
# Withy runtime/task/knowledge data — program- or agent-authored content, not source to format
.withy/
packages/app/next-env.d.ts
*.tsbuildinfo
*.log

# Machine-generated lockfile (not Prettier's to format)
pnpm-lock.yaml

# CLI scaffolding templates: contain {{TOKEN}} placeholders (not valid YAML/JSON
# pre-render) and are written verbatim at init — Prettier must not touch them.
packages/cli/src/templates/

# Markdown is authored prose (and Prettier re-pads CJK tables unhelpfully) — don't enforce its format
*.md
